import React from 'react'

const SearchFetch = () => {
  return (
    <div>
        {/* determine whether a repo or user is being fetched then display results in relevant format */}
    </div>
  )
}

export default SearchFetch